const express = require('express');

const router = express.Router();

/*   *** test d'une route /api avec envoi d'une réponse en json ***
router.get('/', (req, res, next) => {         // gestion de l'url "/" depuis "localhost/api"
  res.json({ text: 'MusicList API Test' });   // on renvoit une réponse en format json
});
*/

module.exports = router;
